<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Simulated shadow layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#33000000" /> <!-- Semi-transparent color to simulate shadow -->
            <corners android:radius="20dp" />
            <padding
                android:left="4dp"
                android:top="4dp"
                android:right="4dp"
                android:bottom="8dp" /> <!-- Adjust padding to control shadow spread -->
        </shape>
    </item>

    <!-- Actual background with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="20dp" />
        </shape>
    </item>

</layer-list>
